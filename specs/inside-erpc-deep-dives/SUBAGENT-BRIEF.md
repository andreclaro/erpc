# SUBAGENT BRIEF — Inside eRPC deep-dive pages (pages 2–7)

You are building ONE deep-dive docs subpage for the eRPC docs site (Nextra 2.13,
Next 15 pages router) at /Users/andreclaro/Code/00.Personal/erpc. The parent
page /inside-erpc (animated pipeline overview) already exists. Six sibling
subpages are being built in parallel by different agents — you own exactly one.

## Read first (in this order)

1. `specs/inside-erpc-deep-dives/SPEC.md` — the full spec. Your page is the
   §6.x section named in your task. Sections 2–5 (architecture, TimelineRace
   contract, page template) apply to you verbatim.
2. `docs/pages/inside-erpc/request-lifecycle.mdx` — the pattern page (page 1,
   already built). Copy its structure exactly: frontmatter, deep-relative
   imports, `<LLMsTxtLink />`, intro, widget, `## The workflow`,
   `## In the source`, `## First principles` (blockquotes),
   `## Agent reference` (1 PromptExample), AISection (How it works /
   Observability / Source code entry points / Related pages).
3. `docs/components/explainer/TimelineRace.tsx` — the shared timeline player
   (if your page uses it) — its exported interfaces are the contract.
4. `docs/components/explainer/scenarios/request-lifecycle.ts` — scenario-data
   file pattern (if your page uses TimelineRace).
5. `docs/styles/deep-dive.css` — the CSS vocabulary available under `.cv-dd-root`
   (do NOT edit this file; if you need extra classes, define them in a new file
   `docs/styles/dd-<topic>.css` and tell me in your report so I wire the import).

## Files you create (exactly these, no more)

- `docs/pages/inside-erpc/<topic>.mdx` — the page.
- If your page uses TimelineRace (failsafe-in-depth, consensus,
  block-availability): `docs/components/explainer/scenarios/<topic>.ts`
  exporting your scenario(s) — typed against TimelineScenario.
- If your page has a bespoke widget (finality-and-caching → ChainReorg,
  health-and-selection → ScoreTicker, evm-translation → RangeSlicer):
  `docs/components/explainer/<Widget>.tsx` — self-contained, SSR-safe static
  SVG + useEffect GSAP hydration, gsap.timeline killed/rebuilt on Replay,
  prefers-reduced-motion → final state, `.cv-dd-root dd-widget` shell with
  dd-head/dd-cap/dd-replay/dd-stage classes (see deep-dive.css).

Import style inside your MDX (deep-relative, do NOT edit
`docs/components/index.ts` — the parent agent owns it):

```mdx
import { LLMsTxtLink, AISection, SourceLink, PromptExample } from "../../components";
import { TimelineRace } from "../../components/explainer/TimelineRace";
import { yourScenario } from "../../components/explainer/scenarios/<topic>";
```

Note: `Callout` does NOT exist in this codebase — use plain markdown
blockquotes (`> **…**`) for the First principles callouts, exactly like the
pattern page.

## Hard rules

- NEVER duplicate config schema tables — link to the reference pages listed in
  your SPEC §6.x section instead.
- Every default value must be verified against `common/defaults.go` or the
  owning file (grep/read it — never from memory). Every metric name must be
  verified in `telemetry/metrics.go` before citing. Every Go file/function
  reference must be real (check with Grep/Read).
- Animations are stripped from `.llms.txt` ⇒ every animated teaching point MUST
  also exist as markdown prose on the page (workflow steps mirror the animation
  beat-for-beat).
- Tabs indentation in all code files (repo convention). zerolog/Go rules don't
  apply here (docs only).
- Do NOT run `pnpm build` or `pnpm dev` (the parent agent owns the build). You
  MAY run `cd docs && npx tsc --noEmit` to type-check.
- Do NOT edit any existing file except creating your own. No commits.
- No AskUserQuestion — make reasonable decisions from the SPEC and report them.

## Quality bar for your widget data (TimelineRace pages)

- Axis duration and bar/marker times must tell the SPEC story coherently:
  plausible relative timings (e.g. upstream round trip ~34ms, block time 2s for
  the catch-up page, hedge delay marker before the win), lanes ordered like the
  pipeline, every bar/marker label ≤ ~60 chars so it fits the SVG.
- For tabbed scenarios (`scenarios` prop): 2–3 tabs per SPEC.
- Play your own mental CDP check: lane ids in bars/markers must exist in
  `lanes`, `end > start` for all bars, markers within [0, duration].

## Report back (compact)

- Files created; the widget's story in 3 bullets; any defaults/metrics you
  cited (with the file you verified them in); tsc result; anything you
  deferred or decided against the SPEC and why; any CSS you need wired.
